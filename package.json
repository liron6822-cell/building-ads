{
  "name": "building-ads",
  "version": "2.0.0",
  "description": "Course approval dashboard — Firebase Firestore real-time sync",
  "private": true,
  "scripts": {
    "start": "serve -s public -l ${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
